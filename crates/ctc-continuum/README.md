# ctc-continuum

Federated chronal continuum runtime. Regions are admitted into a synthetic
in-memory federation, linked via wormhole portals, and advanced with
`tick_federation` to produce a `ContinuumReport`.
